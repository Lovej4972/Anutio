import React from 'react';
import 'react-native';
import CaptureAchievement from '../../components/Capture/CaptureAchievement';

const CaptureScreen = (props) => {
  return <CaptureAchievement {...props} />;
};

export default CaptureScreen;
