import React from 'react';
import 'react-native';
import AddCaptureToExisting from '../../components/Capture/AddCaptureToExisting';

const AddCaptureToExistingScreen = (props) => {
  return <AddCaptureToExisting {...props}/>;
};

export default AddCaptureToExistingScreen;
